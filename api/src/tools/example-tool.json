{
  "id": "example-echo",
  "name": "Example Echo",
  "description": "Echoes the input; demonstrates run types. Also can call the LLM when runCommand is 'llm'.",
  "inputs": {
    "message": { "type": "string", "description": "Message to echo" }
  },
  "outputs": {
    "content": { "type": "object" }
  },
  "type": "mock",
  "mockResponse": {
    "status": "ok",
    "note": "This is a mock response. Change type to 'llm' and add promptTemplate to use the LLM."
  }
}
