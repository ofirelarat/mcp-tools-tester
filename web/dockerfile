FROM node:20-alpine

WORKDIR /usr/src/app
COPY package.json package-lock.json* ./
RUN npm ci
COPY . .
RUN npm run build
# serve via serve package (or simple static server)
RUN npm i -g serve
EXPOSE 3000

CMD ["serve", "-s", "dist", "-l", "3000"]
